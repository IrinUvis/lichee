import 'package:flutter/material.dart';

const kResetCategoriesTreeViewIcon = Icon(
  Icons.home,
  color: Colors.white,
  size: 40.0,
);

const kReturnToUpperLevelInTreeViewIcon = Icon(
  Icons.undo,
  color: Colors.white,
  size: 40.0,
);

const kEmptyCategoryIcon = Icon(
  Icons.hourglass_empty,
  color: Colors.white,
  size: 100.0,
);

const kCategoryNodeArrow = Icon(
  Icons.arrow_right,
  color: Colors.white,
);

const kCategoryIcon = Icon(
  Icons.category,
  color: Colors.white,
);

const kChannelIcon = Icon(
  Icons.chat,
  color: Colors.white,
);

const kDateIcon = Icon(
  Icons.date_range,
  color: Colors.white,
);

const kTimeIcon = Icon(
  Icons.access_time_filled,
  color: Colors.white,
);